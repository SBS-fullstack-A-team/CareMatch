# 기본: 텍스트 파일 줄바꿈 자동 정규화
* text=auto

# Gradle wrapper — OS별 줄바꿈 고정 (Mac/Linux 팀원 대응)
gradlew        text eol=lf
*.sh           text eol=lf
gradlew.bat    text eol=crlf
*.bat          text eol=crlf

# 바이너리
*.jar          binary
